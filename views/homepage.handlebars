  {{#if allPosts.length}}
  <div class="col-md-6 project-list">
    <h3>Recent Posts:</h3>

    {{#each allPosts as |post|}}
    <div class="row mb-2">
      <div class="col-md-8">
        <h4><a href="/post/{{post.id}}">{{post.title}}</a></h4>
      </div>
    </div>
    {{/each}}
  </div>
  {{else}}
  <div>
      <p>No Posts Yet</p>
  </div>
  {{/if}}